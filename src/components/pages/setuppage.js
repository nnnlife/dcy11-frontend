import React from 'react';
import Typography from '@material-ui/core/Typography';

export default class SetupPage extends React.Component {
    render() {
        return (
            <Typography>{'Setup Page'}</Typography>
        );
    }
}

